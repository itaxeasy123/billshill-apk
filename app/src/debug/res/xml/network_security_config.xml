<?xml version="1.0" encoding="utf-8"?>
<!--
  DEBUG ONLY. Lives in src/debug so it cannot reach a release build.

  Cleartext is permitted for the local dev backend alone: 10.0.2.2 is the host machine as
  seen from the emulator, and 127.0.0.1/localhost cover a device on the same host. Every
  other destination still requires HTTPS.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
    <base-config cleartextTrafficPermitted="false" />
</network-security-config>
